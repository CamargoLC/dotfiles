# ==========================================
# 1. SISTEMA E LANÇADORES
# ==========================================
super + Return
    alacritty

super + d
    rofi -show drun

super + shift + e
    ~/.config/rofi/powermenu.sh

super + shift + r
    bspc wm -r && pkill -USR1 -x sxhkd

# ==========================================
# 2. GERENCIAMENTO DE JANELAS E ESTADOS
# ==========================================
# Fechar/Matar janela (Q = Memória i3wm | W = Padrão bspwm)
super + {shift + q,w,shift + w}
    bspc node -{c,c,k}

# Alternar estados (Tiled, Pseudo, Floating, Fullscreen, Float Toggle)
super + {t,shift + t,s,f,shift + space}
    bspc node -t {tiled,pseudo_tiled,floating,fullscreen,"~"floating}

# Layout Monocle (Maximizado) / Troca com a maior janela
super + {m,g}
    {bspc desktop -l next,bspc node -s biggest.window}

# Flags de janela (Marked, Locked, Sticky, Private)
super + ctrl + {m,x,y,z}
    bspc node -g {marked,locked,sticky,private}

# ==========================================
# 3. FOCO E NAVEGAÇÃO (VIM-LIKE & WORKSPACES)
# ==========================================
# Focar ou mover janela na direção (H J K L)
super + {_,shift + }{h,j,k,l}
    bspc node -{f,s} {west,south,north,east}

# Focar ou mover janela para o Workspace (1-0)
super + {_,shift + }{1-9,0}
    bspc {desktop -f,node -d} '^{1-9,10}'

# Focar em janelas relativas (Próxima/Anterior/Pai/Irmão)
super + {_,shift + }c
    bspc node -f {next,prev}.local.!hidden.window
    
super + {p,b,comma,period}
    bspc node -f @{parent,brother,first,second}

# Alternar Desktops e Histórico
super + bracket{left,right}
    bspc desktop -f {prev,next}.local

super + {grave,Tab}
    bspc {node,desktop} -f last

# ==========================================
# 4. PRÉ-SELEÇÃO DE ÁREA (SPLITS)
# ==========================================
# Direção e Proporção (Ratio)
super + ctrl + {h,j,k,l}
    bspc node -p {west,south,north,east}

super + ctrl + {1-9}
    bspc node -o 0.{1-9}

# Cancelar pré-seleção (Focada / Todas no Desktop)
super + ctrl + {_,shift + }space
    {bspc node -p cancel,bspc query -N -d | xargs -I id -n 1 bspc node id -p cancel}

# ==========================================
# 5. REDIMENSIONAMENTO E MOVIMENTO
# ==========================================
# Expandir
super + alt + {h,j,k,l}
    bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}

# Contrair
super + alt + shift + {h,j,k,l}
    bspc node -z {right -20 0,top 0 20,bottom 0 -20,left 20 0}

# Mover janela flutuante com as setas
super + {Left,Down,Up,Right}
    bspc node -v {-20 0,0 20,0 -20,20 0}

# ==========================================
# 6. UTILITÁRIOS E HARDWARE
# ==========================================
# Captura de Tela (Flameshot: Inteira / Área de Seleção)
{_,shift + }Print
    flameshot {full -c,gui}

# Controle de Áudio
XF86Audio{Raise,Lower}Volume
    pactl set-sink-volume @DEFAULT_SINK@ {+5%,-5%}

XF86AudioMute
    pactl set-sink-mute @DEFAULT_SINK@ toggle

# Brilho da Tela (Acer Nitro 5)
XF86MonBrightness{Up,Down}
    brightnessctl set {+5%,5%-}
